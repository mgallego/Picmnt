{% extends "SFMPicmntBundle::base.html.twig" %}

{% block body %}
    <header>
      <div class="container_12 clearfix">
	<div class="grid_12">
	  <h1>
	    <a href="{{ path('home') }}" title="Picmnt">Picmnt</a>
	  </h1>
	  <nav>
	    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
	    <ul>
	      <li>{{  'Hello %username%' | trans({ '%username%': app.user.getUsername() }) }}</li>
	      <li>|</li>
      	      <li><a href="{{ path('_security_logout') }}">{{ 'Logout' | trans }}</a></li>
	    </ul> 	   
	    {% else %}
	    <ul>
	      <li><a href="{{ path('_security_login') }}" title="login">{{ 'Login' | trans }}</a></li>
	      <li>|</li>
      	      <li><a href="{{ path('fos_user_user_new') }}" title="sigin">{{  'Sign in' | trans }}</a></li>
	    </ul> 	   
	    {% endif %}
	  </nav>
	 </div>
      	
      </div>
     </header>
    <!-- <div id="separator"></div>  -->
     <section id="content">
       {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
       <ul>
	 <li><a href="{{ path('img_random') }}">{{ 'Random Image' | trans }}</a></li>
	 {#<li><a id="fBIframe" href="{{ path('img_upload') }}" title="upload">{{ 'Upload an image' | trans }}</a></li>#}
	 <li><a  href="{{ path('img_upload') }}" title="upload">{{ 'Upload an image' | trans }}</a></li>
       </ul>
       {% endif %}


       {% block content %}
       {% endblock %}     
     </section>

    <footer>

      <ul>
	<li>Blog</li>
	<li>About</li>
	<li>Terms</li>
      </ul>

      <div id="copyright">
	Copyright © 2011 Picmnt
      </div>
    </footer>
 {% endblock %}
